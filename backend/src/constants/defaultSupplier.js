'use strict';

/** UUID fijo: coincide con la migración que inserta el proveedor interno */
const DEFAULT_SUPPLIER_ID = '11111111-1111-4111-8111-111111111111';

const DEFAULT_SUPPLIER_NAME = '3barrilesProv';

module.exports = {
  DEFAULT_SUPPLIER_ID,
  DEFAULT_SUPPLIER_NAME,
};
